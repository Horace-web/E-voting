# Configuration de l'API E-Voting Frontend
# Copiez ce fichier vers .env.local et modifiez les valeurs

# URL de l'API Backend (Laravel)
VITE_API_URL=http://localhost:8000/api

# Mode mock (true = utilise les données simulées, false = appelle le vrai backend)
VITE_USE_MOCK=true

# Environnement
VITE_APP_ENV=development
